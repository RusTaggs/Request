

var mySwiper = new Swiper('.swiper-container', {
    // Optional parameters
    
    
    speed: 400,
    spaceBetween: 100,
  
    // If we need pagination
    pagination: {
      el: '.swiper-pagination',
    },
  
    
  })